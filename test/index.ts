import "./asCurrency.js";
import "./asDateString.js";
import "./distinctTests.js";
import "./isUndefinedTests.js";
import "./EventBus.js";

describe("Setup puppeteer for DOM tests or just run tests within device post-deploy", () => {
  it("", () => {
    // assert.isFunction(asNumber);
    // assert.isFunction(
    //   extendNumericInputBehaviors
    // );
    // assert.isFunction(
    //   extendTextInputBehaviors
    // );
    // assert.isFunction(
    //   getQueryParameter
    // );
    // assert.isFunction(gotoUrl);
    // assert.isFunction(
    //   removeCssRestrictors
    // );
    // assert.isFunction(setCurrency);
    // assert.isFunction(gotoUrl);
    // assert.isFunction(hookupTriggers);
  });
});
